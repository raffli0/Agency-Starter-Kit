---
interface Props {
    class?: string;
    as?: string;
}

const { class: className, as = "div" }: Props = Astro.props;
const Tag = as as any;
---

<Tag class={`mx-auto w-full max-w-6xl px-6 sm:px-8 lg:px-10 ${className ?? ""}`}>
    <slot />
</Tag>
